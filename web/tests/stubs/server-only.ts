/**
 * Vitest-заглушка пакета "server-only": в node-окружении тестов его импорт
 * бросает ошибку, а чистым функциям (composeMetadata) серверный контекст
 * не нужен. Алиас подключается в vitest.config.ts.
 */
export {};
